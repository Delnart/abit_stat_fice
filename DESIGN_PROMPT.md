# Промт для Claude Design (скопіюй усе нижче)

---

Спроєктуй веб-застосунок **«Вступ ФІОТ — статистика в реальному часі»** — публічний дашборд,
який показує живу статистику вступної кампанії на спеціальності факультету інформатики
(F2 «Інженерія програмного забезпечення», F2 «Програмування комп'ютерних ігор», F6, F7).
Дані парсяться з державного порталу ЄДЕБО кожні 10 хвилин. Аудиторія — абітурієнти та їхні
батьки, які нервово перевіряють сайт по 20 разів на день з телефона.

## Сторінки

### 1. Дашборд `/` (головна цінність продукту)
Сітка карток — одна картка = одна конкурсна пропозиція. У картці:
- Назва пропозиції (напр. «F2 Інженерія програмного забезпечення») + бейдж бюджет/контракт.
- Головна цифра — **кількість поданих заяв** (велика, помітна) і її зміна за сьогодні (напр. «+124 за день», зеленим/сірим).
- Блок «Бюджетні місця»: всього / квота-1 / квота-2 / загальний конкурс.
- Три рядки категорій, кожен: назва («Квота 1», «Квота 2», «Загальний конкурс»),
  прогрес «пройшли/місць» (напр. 18/22), **прохідний бал** (напр. 163.556) або стан
  «відсутній» (коли місця не заповнені — це ДОБРА новина для абітурієнта, не помилка;
  покажи це нейтрально-позитивно, не червоним).
- Мікроспарклайн динаміки заяв за останні 7 днів.
Над сіткою — глобальний рядок стану: «Оновлено 3 хв тому • наступне оновлення ~за 7 хв» з
пульсуючим індикатором live, і бейдж режиму даних: «Прогноз» (до оголошення рекомендацій,
з тултіпом-поясненням, що це оцінка зверху) або «Офіційні рекомендації» (після 6 серпня).

### 2. Сторінка пропозиції `/offer/[id]`
- Шапка з тими ж ключовими метриками, що на картці.
- Графік: динаміка кількості заяв і прохідних балів у часі (лінійний, з перемикачем метрики).
- Гістограма розподілу конкурсних балів заявників (де «я» на цій кривій).
- Таблиця рейтингового списку: № , бал, пріоритет, маркери КВ1/КВ2, статус заяви, маркер
  «оригінал документів». Пошук/фільтр, sticky-заголовок, віртуалізація для 2000 рядків.
  На мобільному таблиця перетворюється на компактні рядки-картки.

## Стани (обов'язково промалювати)
- Перше завантаження — skeleton-картки.
- «Дані застаріли» (>30 хв без оновлення) — жовтий ненав'язливий банер.
- Помилка парсингу однієї пропозиції — картка лишається з останніми даними + позначка часу.
- Порожній стан до старту кампанії.

## Стиль
- Data-dense дашборд у дусі Linear/Vercel: спокійний, точний, без «дитячих» ілюстрацій.
- Темна тема за замовчуванням + світла (перемикач). Проєкт виріс із термінального бота —
  можна тонко підморгнути моноширинним шрифтом для ЦИФР (tabular nums обов'язково),
  але це сучасний веб-продукт, не емуляція терміналу.
- Один акцентний колір для live/оновлень; зелений/жовтий тільки семантично.
- Мова інтерфейсу — українська. Числа: бал з трьома знаками після коми (163.556),
  тисячі з тонким пробілом (1 932).
- Mobile-first: 70 % трафіку — телефони. Картки в одну колонку, ключові цифри читаються
  з витягнутої руки.
- Доступність: контраст AA, статуси не тільки кольором (текст/іконка).

## Технічні рамки
- Next.js (App Router) + Tailwind CSS, статичний експорт — без серверних компонентів з даними.
- Дані приходять клієнтом із REST API (SWR, оновлення раз на хвилину) — покажи, як виглядає
  плавне оновлення цифри без стрибків лейауту.
- Графіки — легка бібліотека (Recharts або нативний SVG-спарклайн).
- Без зовнішніх шрифтів-CDN, які блокуються — системний стек або self-hosted Inter.

Віддай: макет дашборда (desktop + mobile), сторінку пропозиції (desktop + mobile),
токени теми (кольори/типографіка/відступи) і компоненти картки в усіх станах.

---
