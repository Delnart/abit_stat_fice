# План: сайт статистики вступу на наші спеціальності (ФІОТ)

Стек: **Next.js (фронт) + NestJS (бек) + MongoDB Atlas + Render Free**.
Мета: автоматично парсити vstup.edbo.gov.ua по нашим конкурсним пропозиціям і показувати
метрики як на скріншоті (заяви, місця, «пройшли», прохідні бали по квоті-1/квоті-2/загальному
конкурсу) з оновленням у близькому до реального часі.

---

## 1. Джерело даних — ключове відкриття (перевірено наживо 20.07.2026)

Портал **vstup.edbo.gov.ua у 2026 переписали на Next.js 15**. Старий JSON API
(`/offers-list/`, `/offer-requests/`), який використовують усі відомі парсери з GitHub,
**на живому хості мертвий** (HTTP 308). Але:

- **`GET https://vstup.edbo.gov.ua/offer/{id}`** — звичайний GET без капчі, кукі та
  спецзаголовків — повертає ~300 КБ SSR-HTML, у якому **весь список заяв вбудований як JSON**
  у чанках `self.__next_f.push(...)` (масив `requests`). Headless-браузер не потрібен:
  fetch → знайти чанки → unicode-unescape → `JSON.parse` → масив заяв.
- Поля кожної заяви: `konkurs_value` (конкурсний бал), `priority`, `q1`, `q2` (маркери квот),
  `person_request_status_id` (Допущено/Зареєстровано/Рекомендовано/…), `is_claim_for_budget`,
  `has_original_documents`, `subjects[]` (предмети з коефіцієнтами). Пагінації немає — всі
  заяви в одному документі.
- **Rate-limit не виявлено**, але ~7–13 % запитів випадково падають з 500/502 —
  **обовʼязково 2–3 ретраї з невеликим бекофом**.
- Пошук оферів на сайті захищений Turnstile-капчею + шифрованим server action —
  **не відтворюємо**. Робимо простіше: ID наших оферів (їх ~4–8) один раз на кампанію
  збираємо руками з браузера (посилання виду `/offer/1589232`) і кладемо в конфіг.
- Історичні дані (2022–2025) за потреби: старий API живий на архівних хостах
  `vstup{YEAR}.edbo.gov.ua` (`POST /offer-requests` body `id={}&last={}`,
  заголовок `X-Requested-With: XMLHttpRequest`).
- Тестова фікстура вже в репо: `fixtures/offer-1589232-2026-07-20.html` — реальна сторінка
  офера з 64 заявами. Парсер пишемо і тестуємо проти неї, без мережі.

**Ризик №1 проєкту:** ЄДЕБО задеплоїть нову версію і формат `__next_f` зміниться.
Тому парсер = окремий чистий модуль з тестом на фікстурі + алерт при невдалому парсі.

---

## 2. Архітектура (підігнана під ліміти Render Free)

Ліміти Render Free (перевірено по докам, липень 2026):
- **750 інстанс-годин/міс на весь воркспейс** → цілодобово може жити рівно **один** веб-сервіс.
- Free web service засинає після 15 хв без трафіку, холодний старт ~1 хв.
- **Cron jobs на Render платні** (від $1/міс).
- **Static sites — безкоштовні й не їдять інстанс-години.**
- MongoDB на Render немає → **MongoDB Atlas Free/M0** (512 МБ, ~100 ops/s) — стандартна практика.

Тому:

```
cron-job.org (безкоштовно, кожні 10 хв)
      │  GET /api/cron/scrape?token=SECRET   ← один механізм = і тригер парсингу, і keep-alive
      ▼
NestJS API ──► vstup.edbo.gov.ua/offer/{id}  (fetch + ретраї, 1 запит/сек)
 (єдиний        │ parse __next_f → requests[]
  free-сервіс)  │ розрахунок статистики
      │         ▼
      │    MongoDB Atlas M0  (snapshots + latest)
      ▼
REST API  ◄── SWR-полінг кожні 60 с ── Next.js (static export на Render Static Site, CDN)
```

- Зовнішній крон **замість** `@nestjs/schedule`: після холодного старту внутрішній таймер
  «зсувається», а зовнішній пінг і будить сервіс, і запускає парсинг — два зайці одним
  запитом. Ендпоінт захищений секретом у query/заголовку.
- «Реальний час» = SWR `refreshInterval: 60_000` на фронті. Джерело саме оновлюється раз на
  ~10 хв, тож полінг раз на хвилину — чесний real-time без WebSocket-складнощів.
  (SSE — можливий апгрейд пізніше, Render його підтримує.)
- Next.js зі `output: 'export'` → деплой як **Render Static Site** (безкоштовно, без годин),
  усі дані тягне клієнтом з API.

---

## 3. Структура репозиторію

Монорепо без оркестраторів (turborepo/nx не потрібні для 2 пакетів):

```
stat_abit_bot/
├── apps/
│   ├── api/        # NestJS
│   └── web/        # Next.js (output: 'export')
├── fixtures/       # збережені HTML-сторінки оферів для тестів парсера
├── PLAN.md
└── DESIGN_PROMPT.md
```

---

## 4. Модель даних (MongoDB Atlas M0, 512 МБ)

Головне обмеження: сирий список заяв великий (1932 заяви × ~1 КБ). Якщо зберігати його в
кожному знімку — ~280 МБ/день, база лусне за 2 дні. Тому:

- **`offers`** — конфіг і метадані (upsert при кожному парсі):
  `{ offerId, title, faculty, seatsMax, seatsQ1, seatsQ2, enabled, sortOrder }`
- **`applicants_latest`** — останній сирий список по оферу (**перезапис**, не історія):
  `{ offerId, fetchedAt, requests: [...] }` → ~2 МБ на всі офери, константа.
- **`snapshots`** — історія **тільки розрахованих метрик** (для графіків динаміки):
  `{ offerId, ts, total, budget: { q1: {seats, passed, cutoff}, q2: {...}, general: {...} },
     parseOk }` → ~150 байт × 8 оферів × 144/день × 45 днів ≈ дрібниці.
  Індекс `{offerId: 1, ts: -1}`.

---

## 5. Розрахунок статистики (як на скріншоті)

Правила Вступ-2025/2026 (Порядок прийому, наказ МОН №168, Додаток 9 — перевірено):

- Квота-1 = 10 % макс. обсягу (округлення, мін. 1 місце); квота-2 = 10 % (40 % для
  прифронтових областей). **Невикористані місця квот не згорають** — віддаються
  загальному конкурсу.
- До симуляції бюджету беремо заяви: тип «Б» (`is_claim_for_budget=1`) + статус
  **«Допущено до конкурсу»**; на старті кампанії, поки більшість ще «Зареєстровано»,
  включаємо і їх з позначкою «прогноз».
- Сортування в межах категорії: бал ↓ → пріоритет ↑ → оцінка головного предмета ↓.
- Пооферна симуляція: кишеня КВ1 (seats = квота-1), кишеня КВ2, решта — загальний конкурс
  (seats = макс − зайняті квотні). «Пройшли за X» = розмір кишені після розкладання.
- **Прохідний бал категорії** = бал останнього, хто помістився, — **тільки якщо місця
  заповнені повністю**; якщо пройшло менше, ніж місць → «відсутній» (як «18 з 22» на
  скріншоті).
- Чесність: до оголошення рекомендацій (~06.08) пооферна симуляція — **оцінка зверху**
  (не бачить, що людина піде в інший ЗВО за вищим пріоритетом; глобальний алгоритм
  ЄДЕБО — deferred acceptance по всій країні). Показуємо в UI бейдж «прогноз».
  Після публікації рекомендацій перемикаємось на **факт**: рахуємо по статусах
  «Рекомендовано» і маркерах КВ1/КВ2 — це вже точні цифри.

---

## 6. Бекенд (NestJS) — мінімум модулів

- **`ScraperModule`**
  - `EdboClient` — fetch офера з ретраями (3 спроби, backoff 1s/3s), таймаут 15 с,
    звичайний desktop User-Agent, між оферами пауза ~1 с.
  - `EdboParser` — чистa функція `html → Request[]` (легко тестується на фікстурі).
  - `StatsCalculator` — чиста функція `Request[] + seats → OfferStats` (алгоритм з п.5).
  - `ScrapeService.runAll()` — по всіх enabled-оферах: fetch → parse → calc → upsert
    `applicants_latest` + insert `snapshot`. Помилка парсингу одного офера не валить решту
    (snapshot з `parseOk: false`).
- **`ApiModule`** (публічні GET, глобальний ThrottlerGuard):
  - `GET /api/offers` — останні метрики всіх оферів + `updatedAt` (головний ендпоінт дашборда).
  - `GET /api/offers/:id/history?from=&to=` — часовий ряд снапшотів для графіків.
  - `GET /api/offers/:id/applicants` — поточний рейтинговий список (для сторінки офера).
  - `GET /api/cron/scrape?token=` — тригер парсингу (звіряє `CRON_SECRET`; ігнорує виклик,
    якщо попередній ще виконується).
  - `GET /healthz` — для UptimeRobot як резервного пінгера.
- Конфіг через env: `MONGO_URI`, `CRON_SECRET`, `OFFER_IDS=1589232,1589233,…`
  (ID зібрані руками з браузера один раз на кампанію).

## 7. Фронтенд (Next.js, static export)

- `next.config.js`: `output: 'export'` → тека `out/` → Render Static Site.
- Дані: SWR, `refreshInterval: 60_000`, `NEXT_PUBLIC_API_URL` → NestJS.
- Сторінки:
  - **`/` — дашборд**: сітка карток по оферах, 1-в-1 метрики скріншота + «оновлено N хв
    тому» + бейдж «прогноз/факт». Це 90 % цінності сайту.
  - **`/offer/[id]`** — деталі: графік динаміки (заяви й прохідні в часі — з `snapshots`),
    гістограма розподілу балів, рейтинговий список з пошуком по номеру заяви.
- Стани: перше завантаження (skeleton), «дані застарілі > 30 хв» (жовтий бейдж —
  означає, що крон або ЄДЕБО лежить), помилка парсингу офера.
- Дизайн — за промтом із `DESIGN_PROMPT.md` (Claude Design), Tailwind.

## 8. Деплой (усе безкоштовно)

1. **MongoDB Atlas** — Free-кластер (M0), user + IP allowlist `0.0.0.0/0`
   (Render free не має статичних IP), `MONGO_URI` в env.
2. **Render Web Service** (apps/api): build `npm ci && npm run build`, start
   `node dist/main`. Free instance (512 МБ / 0.1 CPU). Env: `MONGO_URI`, `CRON_SECRET`,
   `OFFER_IDS`.
3. **Render Static Site** (apps/web): build `npm ci && npm run build`, publish dir `out`,
   env `NEXT_PUBLIC_API_URL`. CDN, не їсть інстанс-години.
4. **cron-job.org** — job кожні 10 хв на `https://<api>.onrender.com/api/cron/scrape?token=…`.
   Це і парсинг, і keep-alive (сервіс ніколи не засинає; ~744 год/міс — влазить у ліміт 750,
   бо статичний сайт годин не споживає).
5. **UptimeRobot** (опційно) — монітор на `/healthz` як резерв і алерт, якщо API впав.

## 9. Фази робіт

**Фаза 1 — парсер + розрахунок (ядро, без мережі).**
Скелет монорепо; `EdboParser` і `StatsCalculator` як чисті функції; тести на
`fixtures/offer-1589232-2026-07-20.html`; звірити цифри з тим, що показує сайт ЄДЕБО.
✅ Критерій: з фікстури виходять правильні «подано/пройшли/прохідний».

**Фаза 2 — бекенд цілком.**
NestJS + Mongoose, `EdboClient` з ретраями, `ScrapeService`, всі GET-ендпоінти, throttling.
✅ Критерій: локально `GET /api/cron/scrape` наповнює Atlas, `GET /api/offers` віддає метрики
живих оферів.

**Фаза 3 — фронтенд.**
Дашборд + сторінка офера за макетом від Claude Design; SWR-полінг; стани
loading/stale/error. ✅ Критерій: локально бачу живі картки, як на скріншоті.

**Фаза 4 — деплой.**
Atlas → Render API → Render Static → cron-job.org → UptimeRobot. Прогнати добу, подивитись
логи ретраїв. ✅ Критерій: сайт живе на *.onrender.com, дані самі оновлюються кожні 10 хв.

**Фаза 5 — після запуску (за бажанням).**
Графіки динаміки прохідного; перемикач «прогноз/факт» після 06.08; архівні роки через
`vstup{YEAR}.edbo.gov.ua`; SSE замість полінгу; Telegram-алерти про зміну прохідного.

## 10. Ризики та що з ними робимо

| Ризик | Мітигація |
|---|---|
| ЄДЕБО змінить формат `__next_f` / розмітку | Парсер — ізольована чиста функція + тест на фікстурі; snapshot з `parseOk:false` + жовтий бейдж на сайті; фікс за годину |
| Випадкові 500/502 від ЄДЕБО (~10 %) | 3 ретраї з бекофом — уже в дизайні клієнта |
| ЄДЕБО введе rate-limit / бан IP | У нас 4–8 запитів раз на 10 хв з паузою 1 с — найтихіший можливий профіль |
| Render Free: 750 год/міс | Рівно один always-on сервіс; фронт — static site (0 годин) |
| Прохідний до 06.08 неточний за визначенням | Бейдж «прогноз», після рекомендацій — рахунок по факту статусів |
| Atlas M0 512 МБ | Історія тільки метрик (снапшоти ~КБ), сирі списки — лише latest |
| ID оферів змінюються щороку | `OFFER_IDS` в env; оновлення — 5 хвилин руками на старті кампанії |
